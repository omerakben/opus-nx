# ============================================================
# Opus Nx — Local Docker Configuration
#
# Only ANTHROPIC_API_KEY is required.
# Everything else is pre-configured for docker-compose.local.yml.
#
# Setup:
#   cp .env.docker .env
#   # Add your Anthropic API key below
#   docker compose -f docker-compose.local.yml up -d
#   pnpm dev
# ============================================================

# Required: Your Anthropic API key (https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# Login password & auth signing key
AUTH_SECRET=opus-nx-local-dev

# ── Local Database (pre-configured — do not change) ─────────
# These match the JWT tokens generated for the local PostgREST instance.
# They use Supabase's well-known local dev secret and are safe for local use.

SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# ── Swarm ────────────────────────────────────────────────────

NEXT_PUBLIC_SWARM_URL=http://localhost:8000

# ── Optional ─────────────────────────────────────────────────

DEMO_MODE=false
# VOYAGE_API_KEY=pa-...
# TAVILY_API_KEY=tvly-...
