# Opus NX Agent Swarm — Python Backend
# Copy this file to agents/.env and fill in your own values.

# Required
ANTHROPIC_API_KEY=sk-ant-...
AUTH_SECRET=change-me            # Must match the Vercel/Next.js AUTH_SECRET exactly
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=...

# Optional semantic retrieval provider
# VOYAGE_API_KEY=pa-...

# CORS — allowed origins for cross-origin requests
# Default includes localhost:3000 and opus-nx.vercel.app
# Override to add custom domains:
# CORS_ORIGINS=["http://localhost:3000","https://opus-nx.vercel.app"]

# Neo4j Graph Database (Optional — swarm works without it)
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=...
